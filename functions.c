#include "monty.h"
void pall_func(stack_t **stack, unsigned int num)
{
	if (stack && num)
	{
		;
	}
}
void push_func(stack_t **stack, unsigned int num)
{
	if (stack && num)
        {
                ;
        }

}
